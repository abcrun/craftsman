import * as constants from './constants';
import * as views from './views';
import * as actions from './actions';
import reducer from './reducer';

export default { constants, views, actions, reducer };
